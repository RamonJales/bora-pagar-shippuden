services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=borapagar'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
